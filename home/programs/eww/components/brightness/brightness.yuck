(defvar brightness 50)
(defvar brightness_text "50%")
(defvar brightness_icon "󰃠")

(defwindow brightness-osd
  :monitor 0
  :stacking "overlay"
  :focusable false
  :exclusive false
  :geometry (geometry
    :x "0px"
    :y "0px"
    :anchor "center"
    :width "360px"
    :height "84px")
  (brightness-osd-widget))

(defwidget brightness-osd-widget []
  (box :class "osd-card brightness-osd"
    :orientation "v"
    :space-evenly false
    :valign "center"
    :spacing 10
    (label :class "osd-label brightness-label" :halign "center" :text "${brightness_icon} ${brightness_text}")
    (progress :class "osd-progress brightness-progress" :value brightness :min 0 :max 100)))
